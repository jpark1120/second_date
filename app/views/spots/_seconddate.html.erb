<%= form_for second, url: spot_flags_path(@spot.id) do |f| %>
  <%= f.hidden_field :second_date, { value: "true" } %>
  <%= f.hidden_field :user_id, { value: current_user.id } %>
  <%= f.hidden_field :spot_id, { value: @spot.id } %>
  <%= f.submit "Second Date" %>
<% end %>

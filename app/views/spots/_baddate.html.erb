<%= form_for bad, url: "/spots/:spot_id/flags" do |f| %>
  <%= f.hidden_field :fourth_date, { value: "true" } %>
  <%= f.hidden_field :user_id, { value: current_user.id } %>
  <%= f.hidden_field :spot_id, { value: @spot.id } %>
  <%= f.submit "Bad Date" %>
<% end %>
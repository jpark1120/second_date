<%= form_for third, url: "/spots/:spot_id/flags" do |f| %>
  <%= f.hidden_field :third_date, { value: "true" } %>
  <%= f.hidden_field :user_id, { value: current_user.id } %>
  <%= f.hidden_field :spot_id, { value: @spot.id } %>
  <%= f.submit "Third Date" %>
<% end %>
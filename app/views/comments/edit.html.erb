<%= form_for @comment do |f| %>
  <p><%= f.text_area :opinion, { value: @comment.opinion , size: "35x15" } %></p>
  <%= f.hidden_field :user_id, { value: current_user.id } %>
  <%= f.hidden_field :spot_id, { value: @comment.spot.id} %>
  <p><%= f.submit %></p>
<% end %>
<%= form_for "/spots/#{params[:spot_id]}/comments", method: "POST" do |f| %>
  <p><%= f.text_area :opinion, { placeholder: "Comments", size: "35x15" } %></p>
  <%#= f.hidden_field :user_id, { value: current_user.id } %>
  <%#= f.hidden_field :spot_id, { value: @spots.id } %>
  <p><%= f.submit "Save" %></p>
<% end %>